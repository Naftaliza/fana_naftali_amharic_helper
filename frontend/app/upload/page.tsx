import { UploadExperience } from "@/components/UploadExperience";

// /upload renders the same experience as the home page (kept for existing links).
export default function UploadPage() {
  return <UploadExperience />;
}
